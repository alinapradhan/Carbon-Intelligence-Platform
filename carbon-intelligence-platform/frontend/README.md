# Frontend

React, TypeScript, TailwindCSS, and Recharts dashboard for the Carbon Intelligence Platform.

```bash
npm install
npm run dev
```

Set `VITE_API_URL` if the FastAPI backend runs on a non-default URL.
